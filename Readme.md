# Integration Runtime self-hosted node installation

This script can be used as a VM extension when deploying an Azure VM to automate the installation of an Integration Runtime self-hosted node. 

## 
